"""Expensive evaluation metrics run periodically from the training loop.

TODO: add ``whisper_embed.py`` with a cosine-distance metric between the
Whisper encoder embeddings of target vs. predicted audio.
"""
